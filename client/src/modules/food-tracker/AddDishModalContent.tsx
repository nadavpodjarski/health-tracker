import React, { FC } from "react";
import { Direction } from "../../main/types";
import { langs } from "../../main/languages/app-dictionary";

const AddDishModalContent: FC<Direction> = ({ direction }) => {
  return (
    <div>
      <h1>Content</h1>
    </div>
  );
};

export default AddDishModalContent;
